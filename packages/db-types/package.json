{
  "name": "@platform/db-types",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "gen": "echo 'Run: supabase gen types typescript --project-id <id> > src/database.ts'",
    "clean": "rimraf dist .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "@platform/config": "workspace:*",
    "typescript": "^5.7.3",
    "rimraf": "^6.0.1"
  }
}
